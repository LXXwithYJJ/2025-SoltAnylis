# 搜索页面使用说明

## 功能特性

### 1. 智能搜索
- **快速搜索**: 在主搜索框中输入关键词，支持搜索标题、摘要和关键词
- **高级筛选**: 展开高级筛选面板，可以进行多维度精确检索

### 2. 筛选条件
- **成果类型**: 支持筛选论文、专利、奖项
- **发表年份**: 可设置年份范围进行筛选
- **研究机构**: 支持选择特定机构的成果
- **排序方式**: 支持按相关度、被引次数、发表时间排序，可选升序或降序

### 3. 搜索结果展示
- **分页显示**: 支持10/20/50条每页
- **详细信息**: 
  - 论文: 显示期刊、被引次数、关键词、摘要
  - 专利: 显示专利号、公开日期、关键词、摘要
  - 奖项: 显示基本信息
- **相关度标记**: 显示每条结果的相关度百分比
- **引用标记**: 标注是否可获取引用格式

### 4. 相关推荐
- 基于当前搜索结果，智能推荐相关成果
- 推荐列表不分页，显示最相关的5条结果

### 5. 统计分析
- **成果类型分布**: 饼图展示当前搜索结果中各类型成果的占比
- **年份分布**: 柱状图展示成果的年份分布趋势

## 技术实现

### 数据Mock
由于没有后端，所有数据通过 `src/mock/searchMock.ts` 生成：
- 自动生成50篇论文、30个专利、10个奖项的模拟数据
- 支持按条件筛选、排序、分页

### 接口定义
- **请求**: `POST /api/search/criteria`
- **类型定义**: `src/types/search.ts`
- **API封装**: `src/api/search.ts`

### UI组件
- 使用 Naive UI 组件库
- 使用 ECharts (vue-echarts) 进行数据可视化
- 响应式设计，适配不同屏幕尺寸

## 使用示例

### 基础搜索
1. 在搜索框输入 "深度学习"
2. 点击搜索按钮或按回车键
3. 查看搜索结果列表

### 高级搜索
1. 展开"高级筛选"面板
2. 选择成果类型（如：仅论文）
3. 设置年份范围（如：2020-2024）
4. 选择研究机构（如：北京航空航天大学）
5. 设置排序方式（如：按被引次数降序）
6. 点击搜索

### 查看统计
搜索后自动显示：
- 成果类型分布饼图
- 年份分布柱状图

## 扩展建议

当后端API可用时，只需修改以下文件：

1. **src/api/search.ts**: 
   - 将 `mockSearchByCriteria` 替换为真实的API调用
   - 配置正确的baseURL

2. **vite.config.ts**:
   - 取消注释proxy配置
   - 设置正确的后端地址

3. **SearchView.vue**:
   - 将 `import { mockSearchByCriteria }` 改为 `import { searchByCriteria }`
   - 更新调用代码
